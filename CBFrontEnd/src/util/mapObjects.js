// helper function that takes in a react component and a json object
// and returns a render of the keys and values of each component wrapped in that passed component
// function mapObjects(component, Object) {
//   return comp;
// }
